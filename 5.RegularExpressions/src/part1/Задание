1.	Написать шаблон, ищущий все 5-буквенные слова.

2.	Написать шаблон, ищущий все слова от 2 до 5 букв.

3.	Написать шаблон, который ищет все 5-буквенные слова (но состоит только из букв, без цифр и прочего).

4.	Переписать пример [-]?[0-9]+([.,][0-9]+)?, находящий все числа (и дроби тоже), но с помощью другого синтаксиса (подумать, как).

5.	Написать валидатор для email: сначала идет последовательность символов от 1 до 10, потом @, потом набор (mail | gmail |yandex), потом точка, в конце – (ru | com).

6.	Написать шаблон, ищущий любые слова, но в которых нет дефиса.

7.	Дана строка, содержащая названия файлов («file.txt  file.exe filwfad.zip»). Получить только названия файлов (прочие строки, не являющиеся названиями файлов, должны быть проигнорированы).

8.	Дана строка, состоящая из текста и цифр. Заменить все цифры на («[цифра]»).

9.	Написать валидатор для URL. Начинаться может как с www, так и с http (s). Заканчиваться может только на ru или com.

10.	Написать валидатор для номера банковской карты.

11.	Написать валидатор для IP адреса (простой).

12.	Написать валидатор для пароля: хотя бы одна цифра, хотя бы один спецсимвол, хотя бы одна заглавная и строчная буква, количество символов – от 5 до 20.

13.	* Изменить задание 12 так, чтобы в пароле не было ни одного восклицательного знака.
